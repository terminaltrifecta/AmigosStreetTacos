import React, { Children } from 'react'

function Tab(props: any) {
  
  const text = props.text;
  const img = props.img;
  const side = props.side;

  
  return (
   <>
   </>      
    )

}

export default Tab;